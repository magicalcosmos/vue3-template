'use strict';
module.exports = {
  // API address
  API_BASE: 'return `${window.location.protocol}//${window.location.host}/api/v2`',
  // Websocket address
  WS_ENDPOINT: 'return `ws://${window.location.host}/ws`',
};
